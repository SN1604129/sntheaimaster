{
  "name": "sntheaimaster-root",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter frontend dev\" \"cd backend && uvicorn main:app --host 0.0.0.0 --port 8000\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
